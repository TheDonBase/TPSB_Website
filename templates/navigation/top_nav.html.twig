<nav class="bg-zinc-800/50 backdrop-blur-sm border-b border-zinc-700">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <span class="material-icons text-blue-400 text-2xl">rocket_launch</span>
                </div>
                <div class="hidden md:block">
                    <div class="ml-10 flex items-baseline space-x-4">
                        <a href="{{ path('app_home') }}" class="px-3 py-2 rounded-md text-sm font-medium text-zinc-100 hover:bg-zinc-700">Hem</a>
                        <a href="#" class="px-3 py-2 rounded-md text-sm font-medium text-zinc-300 hover:bg-zinc-700">Om oss</a>
                        <a href="{{ path('app_dashboard') }}" class="px-3 py-2 rounded-md text-sm font-medium text-zinc-300 hover:bg-zinc-700">Dashboard</a>
                    </div>
                </div>
            </div>
            <div class="flex items-center">
                <span class="material-icons text-zinc-400 hover:text-zinc-100 cursor-pointer">account_circle</span>
            </div>
        </div>
    </div>
</nav>
